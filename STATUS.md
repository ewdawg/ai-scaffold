# STATUS.md

> **This is a live snapshot of the current state.** AI agents should read this at the beginning of every session to gain immediate context on what's broken, what's in progress, what's unverified, and what the next concrete action is. Overwrite stale lines; do not treat this file as an append-only history log (use `docs/CHANGELOG.md` for that).

## Current state
- The repository has been freshly initialized from `ai-scaffold`.
- `AGENTS.md` is populated with base templates.
- **Action Required:** A human developer needs to fill out the `[TODO]` sections in `AGENTS.md` and `README.md`.

## In flight
- Initial project scaffolding and planning.

## Known gaps
- Project architecture is undefined.
- Tech stack is undefined.

## Open `[VERIFY:]`
- _(none)_ — list every unresolved `[VERIFY:]` marker an agent has emitted. Promote to "resolved" by replacing the line with verified fact elsewhere, then delete it here.

## Open `[INFERRED:]`
- _(none)_ — list every assumption an agent filled in without explicit user authorization. Same resolution rule: confirm with the user or with code, then delete.

## Lessons
- _(none yet)_ — short list of things future sessions should not have to relearn. One line each.

## Next concrete action
- Fill out the `[TODO]` blocks in `AGENTS.md` and `README.md`.
